/**
 * Legacy DevTools utilities for the deprecated session cache.
 *
 * The bespoke localStorage cache has been removed in favour of Supabase
 * `persistSession`. This module is intentionally left blank so existing
 * imports fail loudly during compilation.
 */

export {}; // Keeps TypeScript happy when the file is imported accidentally.
