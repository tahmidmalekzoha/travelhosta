"use client";

import { FunctionComponent } from 'react';
import GuidesManagement from '../../../components/admin/GuidesManagement';

/**
 * Admin page for managing guides (create, edit, delete)
 */
const GuidesManagementPage: FunctionComponent = () => {
    return <GuidesManagement />;
};

export default GuidesManagementPage;
